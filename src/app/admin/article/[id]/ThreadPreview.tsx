// Deprecated — replaced by PromoPreview.tsx after the pivot to single-tweet promos.
// File retained as an empty stub because the workspace mount disallows deletion.
export const _deprecated = true;
